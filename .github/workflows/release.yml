name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "版本号（如 0.3.0）。手动触发时必填；tag 推送时从 tag 名自动解析。"
        required: false
        default: ""

permissions:
  contents: write
  packages: write

env:
  REGISTRY: ghcr.io

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Resolve version
        id: meta
        run: |
          owner_lc="$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')"
          # Manual dispatch passes a version explicitly; tag pushes derive it from the ref name.
          if [ -n "${{ github.event.inputs.version }}" ]; then
            version="${{ github.event.inputs.version }}"
            version="${version#v}"
            tag="v${version}"
            echo "trigger=manual" >> "$GITHUB_OUTPUT"
          else
            version="${GITHUB_REF_NAME#v}"
            tag="${GITHUB_REF_NAME}"
            echo "trigger=tag" >> "$GITHUB_OUTPUT"
          fi
          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "tag=${tag}" >> "$GITHUB_OUTPUT"
          echo "image=${REGISTRY}/${owner_lc}/quotahub" >> "$GITHUB_OUTPUT"
          previous_tag="$(git tag --list 'v*' --sort=-v:refname | grep -v "^${tag}$" | head -n 1 || true)"
          echo "previous_tag=${previous_tag}" >> "$GITHUB_OUTPUT"

      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Build frontend
        working-directory: frontend
        run: |
          pnpm install --frozen-lockfile
          pnpm build

      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Run backend tests
        working-directory: backend
        run: |
          uv sync --all-groups --frozen
          uv run python -m pytest -q

      - name: Package release assets
        run: |
          chmod +x scripts/*.sh
          mkdir -p dist
          ./scripts/package-uv.sh "${{ steps.meta.outputs.tag }}" dist
          ./scripts/package-source.sh "${{ steps.meta.outputs.tag }}" dist
          (cd dist && sha256sum quotahub-*.zip > SHA256SUMS)

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          build-args: |
            QUOTAHUB_VERSION=${{ steps.meta.outputs.version }}
          tags: |
            ${{ steps.meta.outputs.image }}:${{ steps.meta.outputs.version }}
            ${{ steps.meta.outputs.image }}:latest
            ${{ steps.meta.outputs.image }}:${{ steps.meta.outputs.tag }}

      - name: Render release notes
        run: |
          chmod +x scripts/render-release-notes.sh
          ./scripts/render-release-notes.sh \
            "${{ steps.meta.outputs.tag }}" \
            "${{ steps.meta.outputs.previous_tag }}" > dist/release-notes.md

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.meta.outputs.tag }}
          body_path: dist/release-notes.md
          files: |
            dist/quotahub-${{ steps.meta.outputs.tag }}-uv.zip
            dist/quotahub-${{ steps.meta.outputs.tag }}-source.zip
            dist/SHA256SUMS
